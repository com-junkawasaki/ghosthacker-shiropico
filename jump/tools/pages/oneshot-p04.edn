;; 読切 P.4 — 読切の急所（第3版、2026-08-03）。
;; P.2 の「パン屋のタダ」と、ここの「ネットのタダ」が、ピコの中で**同じもの**になる。
;; 悪意はひとつも無い。無いから誰も止められない——それがこの読切の主題。
;; 最終コマ（b6）でピコが両者を接続し、オーリオが黙る＝めくり。
;;
;; 絵の出自: 旧 P.2 改訂版（cc8f731）で seedance 生成済みの絵をそのまま流用する。
;;           ページ番号が動いただけなので再生成は不要（2026-08-03）。
{:style :urasawa            ; 夜の部屋・定点・tilt なし（浦沢）
 :page  "oneshot P.4"
 :rows
 [[{:id "d1" :beat/weight :large
    :dir "夜。ピコの部屋。灯りは消えていて、画面の光だけが顔を下から照らしている"}]
  [{:id "d2" :dir "画面いっぱいのアニメ。隅に小さく『再生数 284,601』" :sfx "…"}
   {:id "d3" :beat/intensity 0.6 :dir "ピコ、拳を握って画面に向かって身を乗り出す"
    :fuki {:type "jagged" :tail "left" :lines ["うわ、" "サイコーなのだ！"]}}]
  [{:id "d4" :beat/weight :small
    :dir "オーリオが画面のふちに とん と止まる。几帳面な無表情"
    :fuki {:type "square" :tail "right" :lines ["ピコ様。それは" "お金を払って" "観るものです"]}}
   {:id "d5" :dir "ピコ、画面から目を離さない"
    :fuki {:type "oval" :tail "left" :lines ["でも、" "タダで 出てくるのだ"]}}]
  [{:id "d6" :beat/weight :large
    :dir "ピコの横顔。なんでもないことのように言う。オーリオはなにも言えない"
    :fuki {:type "oval" :tail "bottom" :lines ["パン屋の おじさんと" "おなじなのだ"]}}]]}
