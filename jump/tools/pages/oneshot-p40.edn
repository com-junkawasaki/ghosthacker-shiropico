;; oneshot P.40 — jump/oneshot-45p.md から自動生成（md-to-pages.cljs）。
;; **手で調整したら、このヘッダを消す**（--force なしでは上書きされない）。
{:style :togashi :page "oneshot P.40"
 :rows
 [[{:id "p0", :dir "オーリオ、撃ち出されるように シャッターの隙間から 夜へ飛ぶ", :sfx "バサッ"}
   {:id "p1", :dir "ゾンビの波の隙間を、白い一筋が 縫っていく 速い。"}]

  [{:id "p2", :dir "ラット、それを 目で追う", :fuki {:type "wavy", :tail "right", :lines ["なにを——"], :who "ラット"}}
   {:id "p3", :dir "ピコ、大きく息を吸う", :sfx "すぅ————"}]]}
